{
  "name": "intrinsic-design",
  "private": true,
  "version": "1.0.8",
  "description": "Intrinsic Design — tokens, patterns, and themes for Intrinsic Value apps",
  "scripts": {
    "build": "node build.mjs",
    "check": "node build.mjs --check",
    "changelog:now": "node scripts/changelog-now.mjs",
    "changelog:sync": "node scripts/sync-changelog-tags.mjs",
    "docs:dev": "npm run dev --prefix docs-site",
    "docs:build": "npm run build --prefix docs-site"
  }
}
